{% extends 'base.html.twig' %}

{% block title %}Invoice index
{% endblock %}

{% block body %}
	<h1>Invoice index</h1>

	{% include "composants/_searchData.html.twig" with { form: form} only %}
	{{ include('composants/_adminSideBar.html.twig') }}
	{{ form_start(formFiltreStatut) }}
	{{ form_widget(formFiltreStatut) }}
	<button class="btn">{{ button_label|default('Save') }}</button>
	{{ form_end(formFiltreStatut) }}

	<section class="ml-64">
		<table class="table">
			<thead>
				<tr>
					<th>Id</th>
					<th>Name</th>
					<th>Description</th>
					<th>Date</th>
					<th>TypePayment</th>
					<th>Price</th>
					<th>actions</th>
				</tr>
			</thead>
			<tbody>
				{% for invoice in invoices %}
					<tr>
						<td>{{ invoice.id }}</td>
						<td>{{ invoice.name }}</td>
						<td>{{ invoice.description }}</td>
						<td>{{ invoice.date ? invoice.date|date('Y-m-d') : '' }}</td>
						<td>{{ invoice.typePayment }}</td>
						<td>{{ invoice.price }}</td>
						<td>
							<a href="{{ path('app_invoice_show', {'id': invoice.id}) }}">show</a>
						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="7">no records found</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
			 <a href="{{ path('app_invoice_new') }}">Create new</a>
	</section>

	{% endblock %}
