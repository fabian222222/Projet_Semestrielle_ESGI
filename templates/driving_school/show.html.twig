{% extends 'base.html.twig' %}

{% block title %}DrivingSchool{% endblock %}

{% block body %}
    <h1>DrivingSchool</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ driving_school.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ driving_school.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ driving_school.address }}</td>
            </tr>
            <tr>
                <th>Siret</th>
                <td>{{ driving_school.siret }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_driving_school_index') }}">back to list</a>

    <a href="{{ path('app_driving_school_edit', {'id': driving_school.id}) }}">edit</a>

    {{ include('driving_school/_delete_form.html.twig') }}
{% endblock %}
