<form method="post" action="{{ path('app_driving_school_delete', {'id': drivingSchool, 'idS' :drivingSchool}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ drivingSchool) }}">
    <button class="btn">Delete</button>
</form>
